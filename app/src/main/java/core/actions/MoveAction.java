package core.actions;
// add needed imports
import core.Card;
import core.locations.Location;

public class MoveAction {
    // We need the card selected where it was selected from and where we want to move it
    private Card card;
    private Location fromLocation;
    private Location toLocation;
}
