package core.locations;
import core.Card;
import java.util.List;
import java.util.ArrayList;

public class Waste implements Location {
    // waste only needs to contain the cards that we have discarded
    private List<Card> cards;
}
